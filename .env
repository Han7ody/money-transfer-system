# إعدادات قاعدة البيانات (PostgreSQL)
# تأكد أن اسم المستخدم وكلمة المرور يطابقان ما تم إنشاؤه في psql
DATABASE_URL="postgresql://money_transfer_user:han7ody@localhost:5432/money_transfer_db?schema=public"

# مفتاح JWT السري
# استخدم مفتاحاً سرياً عشوائياً قوياً وطويل المدى (أكثر من 32 حرفاً) لأغراض الأمان
JWT_SECRET="your-super-secret-jwt-key-min-32-characters-long"

# إعدادات الخادم
PORT=5000

# إعدادات الواجهة الأمامية (لتجنب مشاكل CORS)
FRONTEND_URL="http://localhost:3000"